//Enums by Mr Enum Guy AKA Jack Malvey
// Include this header in any file that needs to use the enums

#ifndef enum_h
#define enum_h

//all variables start at 0 and go up by one
enum SocialClass{ Crew, First, Second, Third };

enum Gender{Male,Female};

enum Age{Child,Adult};

enum Survival{No, Yes};
#endif 